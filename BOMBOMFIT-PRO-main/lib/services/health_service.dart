class HealthService {
  Future<int> readTodaySteps() async {
    // Ponto de integração com HealthKit (iOS) e Health Connect (Android).
    // Retorna 0 até que as permissões e o plugin nativo sejam configurados.
    return 0;
  }
}
